<h2>Authors</h2>

<% if authors.any? %>
  <div id="authors">
    <% authors.each do |author| %>
      <div class="author">
        <h2><%= author %></h2>
      </div>
    <% end %>
  </div>
<% else %>
  <p class="placeholder">There are no authors yet.</p>
<% end %>